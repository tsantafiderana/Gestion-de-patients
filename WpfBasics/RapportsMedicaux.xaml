<UserControl x:Class="WpfBasics.RapportsMedicaux"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="750" d:DesignWidth="1100"
             Background="#F8FAFC">

    <UserControl.Resources>
        <!-- Style carte -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.08" Color="#000"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style bouton filtre -->
        <Style x:Key="FilterBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#EEF2FF"/>
                                <Setter Property="Foreground" Value="#4F46E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========== STYLES DATAGRID (style projet) ========== -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="Transparent"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="RowHeight" Value="48"/>
            <Setter Property="ColumnHeaderHeight" Value="42"/>
        </Style>

        <!-- Header de colonne -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Cellule -->
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EEF2FF"/>
                    <Setter Property="Foreground" Value="#312E81"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Ligne -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F1F5F9"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EEF2FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ========== HEADER ========== -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <StackPanel>
                <TextBlock Text="Rapports Médicaux"
                           FontSize="26" FontWeight="SemiBold"
                           Foreground="#0F172A"/>
                <TextBlock Text="Statistiques et analyses de l'activité médicale"
                           FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Filtres date -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Border Background="White" CornerRadius="10" Padding="4"
                        BorderBrush="#E2E8F0" BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Aujourd'hui" Style="{StaticResource FilterBtn}" Tag="today" Click="Filter_Click"/>
                        <Button Content="7 jours" Style="{StaticResource FilterBtn}" Tag="7days" Click="Filter_Click"/>
                        <Button Content="30 jours" Style="{StaticResource FilterBtn}" Tag="30days" Click="Filter_Click"/>
                        <Button Content="Cette année" Style="{StaticResource FilterBtn}" Tag="year" Click="Filter_Click"/>
                    </StackPanel>
                </Border>

                <Border Background="White" CornerRadius="10" Padding="12,6" Margin="12,0,0,0"
                        BorderBrush="#E2E8F0" BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Du" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="#64748B"/>
                        <DatePicker x:Name="dpDebut" Width="120" SelectedDateChanged="Date_Changed"/>
                        <TextBlock Text="au" VerticalAlignment="Center" Margin="12,0,8,0" Foreground="#64748B"/>
                        <DatePicker x:Name="dpFin" Width="120" SelectedDateChanged="Date_Changed"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <!-- ========== CARTES RÉSUMÉ ========== -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,24">
            <!-- Consultations -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="CONSULTATIONS" FontSize="12" FontWeight="SemiBold"
                               Foreground="#64748B"/>
                    <TextBlock x:Name="txtTotalConsultations" Text="0"
                               FontSize="28" FontWeight="Bold" Foreground="#0F172A" Margin="0,8,0,0"/>
                    <TextBlock x:Name="txtConsultVariation" Text="+0 %"
                               FontSize="13" Foreground="#10B981" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Patients uniques -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="PATIENTS VUS" FontSize="12" FontWeight="SemiBold"
                               Foreground="#64748B"/>
                    <TextBlock x:Name="txtPatientsUniques" Text="0"
                               FontSize="28" FontWeight="Bold" Foreground="#0F172A" Margin="0,8,0,0"/>
                    <TextBlock Text="patients distincts" FontSize="13" Foreground="#64748B" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- RDV réalisés -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="RDV RÉALISÉS" FontSize="12" FontWeight="SemiBold"
                               Foreground="#64748B"/>
                    <TextBlock x:Name="txtRdvRealises" Text="0"
                               FontSize="28" FontWeight="Bold" Foreground="#0F172A" Margin="0,8,0,0"/>
                    <TextBlock x:Name="txtRdvTaux" Text="Taux de présence : 0 %"
                               FontSize="13" Foreground="#64748B" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Diagnostics -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="DIAGNOSTICS" FontSize="12" FontWeight="SemiBold"
                               Foreground="#64748B"/>
                    <TextBlock x:Name="txtTotalDiagnostics" Text="0"
                               FontSize="28" FontWeight="Bold" Foreground="#0F172A" Margin="0,8,0,0"/>
                    <TextBlock Text="enregistrements" FontSize="13" Foreground="#64748B" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- ========== CONTENU PRINCIPAL ========== -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>

            <!-- Colonne gauche : Tableau des 15 dernières consultations -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                <DockPanel>
                    <!-- Titre + sous-titre -->
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,16">
                        <TextBlock Text="Consultations récentes"
                                   FontSize="16" FontWeight="SemiBold"
                                   Foreground="#0F172A"/>
                        <TextBlock Text="Les 15 dernières consultations enregistrées"
                                   FontSize="13" Foreground="#64748B" Margin="0,2,0,0"/>
                    </StackPanel>

                    <!-- DataGrid stylisé -->
                    <DataGrid x:Name="dgConsultations"
                              Style="{StaticResource ModernDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="DATE"
                                                Binding="{Binding Date, StringFormat=dd/MM/yyyy}"
                                                Width="110"/>
                            <DataGridTextColumn Header="PATIENT"
                                                Binding="{Binding Patient}"
                                                Width="*"/>
                            <DataGridTextColumn Header="MÉDECIN"
                                                Binding="{Binding Medecin}"
                                                Width="150"/>
                            <DataGridTextColumn Header="MOTIF"
                                                Binding="{Binding Motif}"
                                                Width="160"/>
                            <DataGridTextColumn Header="DIAGNOSTIC"
                                                Binding="{Binding Diagnostic}"
                                                Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <!-- Colonne droite : Activité par médecin -->
            <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Activité par médecin"
                               FontSize="16" FontWeight="SemiBold"
                               Foreground="#0F172A" Margin="0,0,0,16"/>

                    <ItemsControl x:Name="lstMedecins">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F8FAFC" CornerRadius="10" Padding="14" Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Avatar -->
                                        <Border Width="40" Height="40" CornerRadius="20"
                                                Background="{Binding CouleurAvatar}" Margin="0,0,12,0">
                                            <TextBlock Text="{Binding Initiales}"
                                                       Foreground="White" FontWeight="Bold"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Nom}" FontWeight="SemiBold"
                                                       Foreground="#0F172A" FontSize="14"/>
                                            <TextBlock Text="{Binding Specialite}" FontSize="12"
                                                       Foreground="#64748B"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                            <TextBlock Text="{Binding NbConsultations}"
                                                       FontWeight="Bold" FontSize="16"
                                                       Foreground="#0F172A" HorizontalAlignment="Right"/>
                                            <TextBlock Text="consult." FontSize="11"
                                                       Foreground="#64748B" HorizontalAlignment="Right"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>